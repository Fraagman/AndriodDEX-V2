---
trigger: always_on
---

I do not accept placeholders. Rewrite this with real working code that compiles and passes the test I described. If you cannot implement it fully, tell me exactly what is blocking you and reduce the scope, but do not leave a stub

CRITICAL INSTRUCTIONS — READ BEFORE WRITING CODE:

1. I do NOT accept placeholder functions, TODO comments, mock data, or hardcoded values that should be dynamic.
2. Every function you write must be COMPLETE and COMPILE. If you use an external API, show the real call with real parameters.
3. If you need to store state, use real persistence (database, file, or in-memory with proper synchronization) — NOT a hardcoded HashMap that resets on every call.
4. If you generate test data, it must come from a deterministic generator or real system state, NOT a static array of fake values.
5. If you cannot implement something fully due to complexity, REDUCE THE SCOPE to something you CAN implement fully. I prefer a smaller working feature over a larger broken one.
6. After every file, provide the EXACT command I should run to verify it compiles and works.
7. If you use a dependency, specify the EXACT version in the config file (Cargo.toml, build.gradle, package.json) — not "latest".

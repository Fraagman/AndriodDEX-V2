syntax = "proto3";
package zc_protocol;

enum SourceType {
    ANDROID_NATIVE = 0;
    VM_WAYLAND = 1;
}

message VideoFrame {
    uint32 width = 1;
    uint32 height = 2;
    uint64 timestamp = 3;
    bytes rgba_data = 4;
    SourceType source = 5;
}
